if(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
    foreach(size 16 24 32 48 64 128)
        install(FILES icon${size}.png DESTINATION ${CMAKE_INSTALL_PREFIX}/share/icons/hicolor/${size}x${size}/apps RENAME heytrack.png)
    endforeach()
endif()
